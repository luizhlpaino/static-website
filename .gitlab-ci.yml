stages: 
  - build
  - test

build website:
  stage: build
  image: node
  script:
    - npm install
    - npm install -g gatsby-cli
    - gatsby build
  artifacts:
    paths:
      - ./public

test artifact:
  stage: test
  image: alpine
  script:
    - grep -q "Gatsby" ./public/index.html

test website:
  stage: test
  image: alpine
  script:
    - npm install
    - npm install -g gatsby-cli
    - gatsby serve
    - curl "http://locahost:9000" | grep -q "Gatsby" ./public/index.html